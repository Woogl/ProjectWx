# 워크플로우 종합 점검 반영

상태: 완료 · 체크리스트 12/12 통과
다음 행동: 남은 확인은 워크플로우 개선 마무리 확인(workflow-wrapup-checks.md)에서 한다.

- 날짜: 2026-09-26
- 계기: claude-obsidian 전환(`wiki-claude-obsidian-migration.md`)을 마친 뒤 사용자가 워크플로우 종합 점검을 요청했다. 서버·실행기, 대시보드 화면, 문서·기록, Wiki 파이프라인 네 갈래로 나눠 읽기 전용으로 점검했고, 심각한 지적은 AI가 직접 다시 확인했다. 결과는 아래 조사 절에 있다.
- 점검 기준(사용자 2026-09-26): 순정을 그대로 쓰고 복잡한 장치는 줄인다(유지보수 때문). 모든 지침은 SSOT를 엄격하게 따른다.
- 사용자 방향(2026-09-26): "일단 가급적이면 플러그인 순정을 따랐으면 해요." Wiki 절차는 claude-obsidian 순정 안내를 기준으로 고치고, 순정에서 벗어나는 곳은 이유와 함께 밝힌다.
- 이미 처리한 것: 대시보드 데이터 이스케이프 회귀(보안 결함)는 전환 작업 범위의 버그 수정으로 고쳤다(`83eea4a94`, 조사 D0).

## 요청

- 요청 · 사용자 2026-09-26

> 워크플로우를 종합적으로 점검해주세요.

- 추가 요청 · 사용자 2026-09-26

> 가능하면 순정을 그대로 쓰고, 복잡한 장치는 줄였으면 해요. 유지보수 때문이에요

> 또한 모든 지침은 SSOT를 엄격하게 준수해야합니다.

- 새 작업 시작 · 사용자 2026-09-26: 점검 결과를 새 작업으로 반영하자는 제안에 "네"

## 질문

| ID | 질문 | 선택지 | 추천 | 답변 |
| --- | --- | --- | --- | --- |
| Q1 | 코드 속 AI 요청문에 무엇을 남기는가 | 결과 칸 형식과 권한 제한 한 줄만 남기고 나머지 규칙 문장은 정본을 가리킴 / 결과 칸 형식만 남기고 권한 제한도 정본을 가리킴 / 현행 유지(단계·결과 칸·권한 제한·웹 전용 약속·evidence 작성법) | 결과 칸 형식과 권한 제한 한 줄만 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q2 | 접수 상태 파일(`test_feedback_*.json`)을 어디에 두는가 | `Saved/Workflow`(Git 밖)로 옮기고 추적 중인 11개를 저장소에서 지움 / 지금처럼 기록 폴더에서 추적하고 결함만 고침 | `Saved/Workflow`로 옮김 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q3 | 화면의 보류·재전송 장치를 어떻게 하는가 | 없앰(전송이 실패하면 입력을 그대로 두고 같은 버튼으로 다시 전달, 중복은 서버의 기록 버전 검사가 막음, 「저장된 요청으로 AI 다시 시도」는 유지) / 유지하고 영구 잠금 결함만 고침 | 없앰 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q4 | Wiki에 HTML 기획서도 수집하는가 | Markdown과 HTML 수집 / Markdown만(현행) | Markdown과 HTML | 사용자 2026-09-26: "Q4는 Markdown만 수집합시다." |
| Q5 | 작업 처리와 Wiki 갱신이 동시에 돌아도 되는가 | 허용하고 정본에 따로 돈다고 적음 / 서로 막아 한 번에 하나만 | 허용 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q6 | Wiki 원자료의 기한(`refresh_due`)을 어떻게 정하는가 | 순정대로 원자료마다 알맞게(일괄 값 없음) / 수집일부터 365일 일괄 / 30일 일괄(지금) | 순정대로 원자료마다 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q7 | 정하기 단계(구현 승인 전)의 추가 요청을 어떤 권한으로 실행하는가 | 읽기 전용 / 모든 명령 허용(현행) | 읽기 전용 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q8 | 완료된 작업에서도 추가 요청을 받는가 | 완료 뒤에는 숨기고 정본의 "(항상)"을 고침(새 문제는 새 작업) / 지금처럼 항상 보임 | 완료 뒤에는 숨김 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |
| Q9 | "Wiki"가 붙은 대시보드 도구 이름(`Wiki-AI.cjs`·`Export-Wiki.ps1`·`wiki-viewer/` 등)을 바꾸는가 | 그대로 둠 / Workflow 이름으로 정리 | 그대로 둠 | 사용자 2026-09-26: "Q6는 플러그인 순정대로 해주시고, 나머지는 모두 추천대로 해주세요." |

- Q1 이유: 정본 131행이 "커밋·푸시·관리자 정책과 CLI 설정 변경은 지시로 막습니다"라고 정해 두었으므로, 전체 권한으로 도는 AI를 막는 그 한 줄만 요청문에 남긴다. 한국어 지시, 표 셀의 `|` 금지, 승인 범위 처리, 작업 트리 밖 수정 금지는 정본(AGENTS.md·process·Wiki/README.md)에 이미 있어 지운다. "선택지 2개 이상", 계획의 구성, "웹 처리에서는 기록을 AI가 고치지 않고 서버가 쓴다"는 요청문에만 있어 정본으로 옮긴 뒤 가리킨다. 9-26에 요청문에 남기기로 한 항목(웹 전용 약속·evidence 작성법 포함)을 줄이는 것이라 다시 묻는다.
- Q2 이유: 이 파일은 PC마다 다른 실행 상태(진행 중, 프로세스 번호)라 저장소에 둘 이유가 없다. 병합 충돌 표식이 하나만 남아도 서버가 뜨지 않고, 옛 프로세스 번호가 다른 프로세스와 겹치면 "다른 AI가 작업 중"이 풀리지 않는다(조사 D3). 사람이 볼 이력은 서버가 기록 Markdown에 이미 남긴다. 「Workflow 개선」에서 "이전 기록을 삭제하지 않는다"고 한 결정을 바꾸는 것이라 묻는다.
- Q3 이유: 전송 응답을 받지 못하면 화면이 요청을 브라우저 저장소에 붙잡고 입력을 잠그는데, 그 요청을 다시 보낼 버튼이 숨는 경우가 있어 저장소를 손으로 지우기 전까지 작업이 막힌다(조사 D5). 서버가 기록 버전(`taskHash`·`expectedRevision`)으로 이미 중복 요청을 막으므로 화면 쪽 장치는 없어도 된다. 정본 134행의 "입력은 보존되며 같은 버튼으로 이어간다"는 그대로 지켜진다.
- Q4 답변: Markdown만 수집한다(첫 수집 범위 결정 유지). HTML 기획서는 수집하지 않는다.
- Q4 이유: 첫 수집 범위(전환 기록 Q3)에서 HTML을 "내용 추출이 안 된다"며 뺐는데, 순정 capture는 `.html`을 텍스트로 받는다(`capture.py`). 지금 해당 문서는 `Docs/CombatDesign/WX_피해_계산_규격서.html` 하나이고 피해 계산식이 들어 있다. PDF·docx·이미지는 순정 규칙대로 추출 불가로 보고만 한다.
- Q5 이유: Wiki 갱신은 사용자 작업 트리와 다른 전용 작업 트리에서 돌아 파일이 겹치지 않는다. 막으려면 두 서비스가 서로의 잠금을 보는 장치가 더 필요하다. 정본 134행의 "한 번에 하나만"이 Wiki 갱신도 가리키는지 지금은 불분명하다.
- Q6 이유: 지금은 원자료 112건의 기한이 모두 10-26이라 10-27(UTC)부터 lint 오류 343건이 나고 모든 Wiki 쓰기가 막힌다(조사 D1). 처음에는 365일 일괄을 추천했지만, 순정 안내는 "신선도는 분야·주장마다 다르다. 원자료마다 알맞은 `refresh_due`를 정하고 일괄 기한을 적용하지 않는다"(autoresearch `program.md:37-40`)여서, 사용자 방향(순정 우선)에 따라 추천을 바꿨다. 순정대로 하면 수집하는 AI가 원자료의 성격(자주 바뀌는 기획서, 완료된 작업 기록, 원본이 없는 옛 결정 노트)에 맞춰 기한을 정하고, 기한이 다가온 원자료는 다시 확인해 순정 트랜잭션 하나로 한꺼번에 갱신한다. 10-26 기한 문제도 이 트랜잭션으로 푼다.
- Q7 이유: 정본은 "사람이 승인하기 전에는 구현하지 않습니다"인데, 지금 추가 요청은 단계와 상관없이 모든 명령 허용으로 돈다. 「Workflow 개선」에서 미결로 남은 판단이다.
- Q8 이유: 완료 직후 열린 작업 진행 패널에 추가 요청이 남아, 완료된 작업에 수정이 이어질 수 있다. 정본 106행과 화면의 완료 안내는 새 문제를 새 작업으로 받는다. 「Workflow 개선」에서 미결로 남은 판단이다.
- Q9 이유: 이 도구들은 웹 화면이 Wiki를 보여 주던 시절의 이름이다. 바꾸면 배치 파일·스킬·문서·테스트를 함께 고쳐야 하고 브라우저에 저장된 대시보드 입력이 한 번 초기화된다. 유지보수 이득에 비해 변경이 크다.

## 구현 계획

추가 질문 없음. 아래 순서로 한다. 괄호 안은 조사 절의 항목이다.

1. Wiki 절차를 순정 기준으로 고치고 기한 문제를 푼다(D1·M5·E1·E2·S2·S4, Q4·Q6).
   - `Wiki/README.md`를 고친다.
     - 수집 판단: 순정 wiki-ingest 1단계대로 SHA-256을 `.raw/.manifest.json`과 원장에 대조한다. 사본만 있고 원장에 없으면 그 사본으로 수집을 잇는다.
     - 기한: 수집하는 AI가 원자료 성격에 맞게 정한다(순정, 일괄 값 없음). 수집 전에 기한이 다가온 원자료를 다시 확인해, 순정 트랜잭션 하나로 한꺼번에 갱신한다.
     - 묶음: "10개 안팎" 대신 순정 문구(예산을 정하고 정해진 첫 묶음부터)를 쓴다.
     - inbox: 복사본을 지우지 않고(순정: 자동 삭제 없음), 커밋은 `Wiki/wiki`·`Wiki/.raw`로 한정한다.
     - lint 관문: 순정 `lint --vault Wiki --strict`의 종료 코드 0으로 판단한다.
     - 순정 방식으로 보탤 것:
       - 새 주장의 근거 위치는 vault 기준 경로로 쓴다.
       - 버전을 올린 뒤 첫 갱신에서 `migrate` 드라이런을 먼저 한다.
       - 이름이 바뀌거나 지워진 원본은 순정 `supersedes`·`deprecated`로 처리한다.
       - 날짜는 UTC로 쓰고, 원본은 바이트 그대로 복사한다.
       - 무인 실행에서는 페이지 삭제·병합·이름 변경을 하지 않고 보고한다.
     - 푸시 거절: 원격의 새 커밋이 `Wiki/`를 건드리지 않았으면 rebase 뒤 다시 푸시한다. 건드렸으면 되돌리고 새 operation ID로 1단계부터 다시 한다.
     - 서술 규칙:
       - 워크플로우 규칙은 요약하지 않고 정본 경로만 적는다.
       - 새 원자료가 기존 결정과 어긋나면 순정 `supersedes`·`contested`로 처리한다.
     - SSOT: 누가 Wiki를 쓰는지, 처리할 AI 고르기는 정본 링크로 바꾼다.
     - 순정 이탈로 밝혀 둘 것: 무인 실행이 드라이런 계획을 스스로 승인한다. 사람 검토 없이 자동으로 갱신하기로 한 전환 결정(D1·D7) 때문이다.
   - 푸시한 뒤 대시보드로 Wiki 갱신을 한 번 돌려 기한 갱신 트랜잭션을 적용한다. `lint --as-of 2026-10-27`이 0건인지 확인한다.
2. 정본과 지침을 SSOT로 정리한다(S1·S2·S3·S6, Q1·Q5·Q7·Q8).
   - `process/index.md`에 옮기거나 더할 것:
     - 요청문에만 있던 규칙: 선택지 2개 이상, 구현 계획의 구성, 웹 처리에서는 AI가 기록을 고치지 않고 결과 칸으로 돌려준다.
     - 표는 절 제목 바로 아래에 둔다.
     - Wiki 갱신은 작업 처리와 따로 돈다(Q5).
     - 정하기 단계의 추가 요청은 읽기 전용이다(Q7).
     - 완료 뒤에는 추가 요청을 받지 않는다(Q8, "(항상)" 문구 수정).
   - `process/index.md`에서 지울 것: 145행의 Wiki 절차 요약은 규칙과 링크만 남기고, 151행의 낡은 메모를 고친다.
   - AI 요청문(`Workflow-TestFeedback.cjs`의 `taskPrompt`와 이어하기 요청문, `Wiki-AI.cjs`의 Wiki 요청문): 결과 칸 형식과 권한 제한 한 줄만 남기고, 나머지 규칙 문장은 지우거나 정본 절을 가리킨다(Q1).
   - 스킬:
     - `comment-cleanup`·`module-review`에서 Wiki 직접 수정 지시와 규칙 사본을 지운다.
     - `pull`의 자동 백업 서술은 정본을 가리키게 하고, 파일 이름을 `SKILL.md`로 바꾼다.
   - 루트 `README.md`에서 Obsidian 여는 법 사본을 지운다.
   - 순정 Gemini CLI 설정 `.gemini/settings.json`(`context.fileName: AGENTS.md`)을 추가해 Gemini도 AGENTS.md를 읽게 한다.
3. 서버·실행기 결함을 고치고 장치를 줄인다(D3·D7·D9·D10·D13·D14·M1·M2, Q2).
   - 접수 상태 파일:
     - `Saved/Workflow/test-feedback/`로 옮기고, 추적 중인 11개를 `git rm`한다.
     - 읽을 수 없는 파일은 건너뛴다.
     - 프로세스 번호 저장·복원과 옛 형식 호환 코드는 지운다.
   - 실패 수정이나 추가 요청이 코드를 바꿨으면, 서버가 사람 항목 `코드 리뷰`를 대기로 되돌린다(정본 112행).
   - 지울 장치:
     - 서버 35분 제한과 `expectedRevision` 검사
     - Gemini `mcp.allowed`
     - 파생 체크리스트 예외(정본 135행과 코드)
     - 쓰지 않는 export와 질문 번호 지시
     - 잠금은 `finally`에서 먼저 푼다.
   - 서버 교체: "바쁘지 않으면 다시 띄운다"로 줄인다. 두 곳의 파일 목록과 `protocol`·`configuration`을 지운다. `Start-WikiAI.ps1`은 시작 전에 접속 정보를 지워, 서버가 뜨지 못하면 페이지가 AI 꺼짐으로 보이게 한다.
   - 래퍼:
     - 저장소 드라이브가 아닌 경로는 `/mnt/<드라이브>`로 넘기고, 역슬래시 상대 경로는 바꿔 넘긴다.
     - 엔진 확인 실패 메시지에 stderr 끝줄을 쓴다.
   - 터미널 인자 금지 문자를 `"`·`%`·`!`·줄바꿈으로 줄인다.
4. 대시보드 화면을 고치고 잔재를 지운다(D5·D8·D11·D12·E4·M3·M4, Q3·Q8).
   - 화면의 보류·재전송 장치와 서버의 재시도 중복 검사(`retryOperationId`)를 지운다. 「저장된 요청으로 AI 다시 시도」는 둔다.
   - 폴링: 서버 기록이 없는 작업의 revision 비교를 고친다. Wiki 폴링은 진행 줄만 갱신하고, 목록은 상태가 바뀔 때만 다시 그린다.
   - 입력:
     - 질문에 「선택 안 함」을 둔다.
     - 브라우저 저장소를 못 써도 메모리 값으로 전송한다.
     - Wiki 갱신·터미널 버튼은 두 번 눌리지 않게 한다.
     - 시각은 현지 시각으로 보인다.
     - 연결 실패는 한국어 안내로 보인다.
   - 작업 진행 패널:
     - 기록의 다음 행동과 승인된 계획(접힘)을 보인다.
     - AI 항목만 실패일 때는 추가 요청을 안내한다.
     - 완료 뒤에는 추가 요청을 숨긴다(Q8).
     - 패널 포커스, 행 버튼 이름, 진행 알림을 보강한다.
   - 지울 잔재: navigation 계산과 그 테스트 단언, frontmatter 처리, "Project Knowledge"·문서 메타 문구, 쓰지 않는 CSS, 좁은 화면 메뉴 격자.
   - 도식은 자체 SVG→이미지 변환 대신 순정 `mermaid.run`으로 그린다.
5. 기록과 설정을 정리한다(D4·D6·E3·S5·M6).
   - `ability-directional-section.md`: 체크리스트 앞 문단을 표 아래로 옮긴다.
   - 모듈 리뷰 기록 WxAI·WxCombat·WxUI: 상태 줄과 체크리스트를 지우고, 결정 원문은 둔다.
   - WxGame: 구현 부분(원문·검증·대기 체크리스트)을 새 작업 기록으로 옮기고, 리뷰는 상태 없이 둔다.
   - 기록 정리(원문과 근거는 보존): 낡은 머리 서술, 완료 기록의 중복 상태 줄, 중복된 추가 요청을 정리한다. 다른 작업의 AI 대기 항목과 빠진 코드 리뷰 행은 그 작업을 이어갈 때 다룬다.
   - `workflow-review.md`의 미결 2건에 이 작업의 Q7·Q8로 정해졌다고 적는다.
   - 전환 기록의 구현 계획을 현재 설계(D9·D11·D12)로 고치고 승인 줄을 지운다. 재승인은 그 작업에서 따로 받는다.
   - 쓰지 않는 `build-doctor/logs`는 추적 파일을 지우고, 로컬 로그는 휴지통으로 보낸다.
   - Routine에 붙은 커넥터 2개를 뗀다.
6. 검증
   - 워크플로우 Node 테스트 4종을 고쳐 돌린다. 새 규칙마다 결함을 넣어 테스트가 잡는지 본다.
   - 그 밖의 검사: Export-Wiki 뒤 뷰어 테스트, 문서 링크 검사, 하네스 검사.
   - 헤드리스 Edge로 대시보드·작업 진행 패널·도식을 1440px과 375px에서 확인한다.
   - Wiki: 1번의 실제 갱신 결과와 `lint --as-of 2026-10-27` 0건.
   - SSOT: 요청문·스킬·README에 규칙 사본이 없는지 검색한다.
   - 테스트 체크리스트 초안
     - AI 항목: 위 검사들
     - 사람 항목: 코드 리뷰, 대시보드 작업 흐름(입력 중 포커스 유지, 질문 선택 해제, 체크리스트 전달), Wiki 갱신 버튼

구현 승인: 이우성 2026-09-26 (대화: "네")

## 테스트 체크리스트

| 항목 | 확인 방법 | 담당 | 결과 | 근거 |
| --- | --- | --- | --- | --- |
| 워크플로우 Node 테스트 | Export-Wiki.ps1 뒤 TestWorkflowTestFeedback·TestWikiProviders·TestWikiViewer·TestWorkflowFeedbackUI | AI | 통과 | 4개 모두 exit 0 |
| 새 규칙 결함 주입 | 규칙마다 결함을 하나씩 넣고 해당 테스트가 실패하는지 본 뒤 되돌림 | AI | 통과 | 3단계 11건(상태 폴더·읽지 못한 파일·Q7·D7·터미널 문자·health·stderr·래퍼 3건 등), 4단계 16건(폴링·목록 다시 그리기·선택 해제·저장소 대체·중복 클릭 2건·완료 뒤 추가 요청·다음 행동·승인 계획·AI 항목 안내·현지 시각·종료 알림·읽기 전용 안내·Wiki 진행 줄·행 버튼 이름·서버 next), 재승인 규칙 2건 모두 테스트가 잡음 |
| 문서 링크 | CheckWikiLinks.ps1 | AI | 통과 | 34개 문서 오류 0 |
| 하네스 | TestAgentHarness.ps1 | AI | 통과 | exit 0(스킬 문구 수정 뒤 다시 실행) |
| Wiki 기한 문제 | 대시보드 Wiki 갱신으로 기한 갱신 적용 → 래퍼로 `lint --vault Wiki --strict`, `--as-of 2026-10-27`, 대조로 `--as-of 2027-12-31` | AI | 통과 | `47f79230b`(원자료 109건 재확인, 자료별 기한). 지금·10-27 모두 이슈 0(131쪽·링크 1630개), 출력의 as_of로 날짜 적용 확인, 대조 날짜는 343건이라 기한이 모두 10-27 뒤 |
| 대시보드 화면 | 헤드리스 Edge CDP 기기 에뮬레이션 1440px·375px에서 대시보드·작업 절차 도식·작업 진행 패널 | AI | 통과 | 가로 넘침 없음(375px에서 작업 절차 문서가 418px로 넘치던 격자를 고침), 도식은 mermaid.run으로 SVG 1개·오류 0, 패널 제목 포커스·다음 행동·접힌 승인 계획·재승인 단계 표시 |
| 작업 기록 해석 | 모든 기록의 체크리스트·질문 표를 task-records.js로 읽음 | AI | 통과 | 해석 오류 0. 모듈 리뷰 6개는 리뷰·참고, WxGame 구현은 wxgame-review-fixes.md(확인 대기 5/8), 전환 기록은 재승인 단계 |
| SSOT 검색 | 요청문·스킬·README·AGENTS.md에서 정본 규칙 문구 검색 | AI | 통과 | 스킬 3개의 한국어 응답 규칙 반복 6곳을 지움. 남은 것은 해당 문서 자체 규칙(Wiki 안내의 갱신 규칙, push의 커밋 메시지 언어, comment-cleanup의 주석 내용 설명) |
| 서버 교체 | Start-WikiAI.ps1로 다시 띄운 뒤 /health와 상태 폴더 확인 | AI | 통과 | health는 identity·busy뿐, Saved/Workflow/test-feedback 생성, 접속 정보 새로 씀 |
| Routine 설정 | RemoteTrigger로 커넥터를 떼고 프롬프트를 줄인 뒤 get | AI | 통과 | 커넥터 0개, 프롬프트는 README 절차를 가리키는 두 문장, outcomes 빈 값, 예약(06:30 KST)·환경·도구·모델 그대로 |
| 후속 수정 | UI 테스트와 새 단언별 결함 주입, Export-Wiki 뒤 뷰어 테스트, 문서 링크, 하네스, 헤드리스 Edge로 작업 절차 도식 읽기 | AI | 통과 | 워크플로우 Node 테스트 4개 통과, 결함 주입 5건(자유 답변 안내·이름, 결과만 기록한 전달의 처리 AI, 완료·진행 중 터미널 버튼) 모두 테스트가 잡음, 문서 링크 34개 오류 0, 하네스 exit 0, 렌더된 도식에 「질문 · 계획 변경」이 있고 옛 이름은 없음·도식 오류 0 |
| 대시보드 작업 흐름 | OpenWorkflow.bat → 작업 진행 패널에서 입력하는 동안 다른 작업이 AI 처리 중이어도 3초마다 포커스·한글 조합이 끊기지 않는다. 질문 선택지는 선택 안 함으로 되돌릴 수 있다. 체크리스트 결과를 전달하면 기록된다. 완료된 작업에는 추가 요청이 없다. | 사람 | 통과 | 이우성 2026-09-26 (대화: "문제 없었으면 통과로 처리합시다", AI 브라우저 점검 결과로 통과). AI 점검: 헤드리스 Edge에서 실제 클릭·입력, 임시 기록 2개로 하고 지움. 질문 선택 해제 11개 통과(처음엔 선택 안 함, 나를 고른 뒤 풀림, 닫았다 열기·새로고침에도 유지, Q1을 비우고 전달하면 「답하지 않은 질문이 있습니다: Q1」로 막힘). 나머지 12개 통과(다른 작업이 처리 중인 목록 폴링 2회 동안 이름 칸 포커스와 한글 조합 유지, 일부 통과는 AI 없이 결과·전달자·상태 줄 2/3이 기록됨, 마지막 통과로 바로 완료, 완료 뒤 추가 요청과 전달 버튼 없음). AI를 부르는 전달은 없었음. |

## 구현 결과 · 2026-09-26

- 커밋: 1단계 `49e2c3a48`·`47f79230b`, 2단계 `66e9f28a1`, 3단계 `b79cfdb60`, 4단계 `711b003b9`, 5단계 `6dc514f69`·`663ece597`와 Routine 설정(API), 6단계는 이 기록을 담은 커밋. 착수 전 보안 결함 수정은 `83eea4a94`(D0).
- 계획 안에서 더한 것: 서버가 사용하지 않게 된 프로세스 번호 콜백(`onSpawn`)도 지웠다. 작업 진행 패널에 다음 행동을 보이려고 서버 기록 읽기에 `next`를 더했고, 지난 접속 정보로 보낸 요청은 새로 열린 화면을 쓰라는 한국어 안내로 돌려준다. 375px에서 문서가 넘치던 읽기 화면 격자를 고쳤다. Routine은 커넥터와 함께 프롬프트도 Q1 기준(규칙은 정본을 가리킴)으로 README 절차를 가리키는 두 문장으로 줄였다. SSOT 검색에서 찾은 스킬의 한국어 응답 규칙 반복을 지웠다.
- 검증 중 고친 것: Q7 규칙을 처음에 "승인도 체크리스트도 없을 때"로 구현해, 계획이 바뀌어 승인 줄을 지운 기록(체크리스트 있음)의 추가 요청이 모든 명령 허용으로 돌 수 있었다. 승인을 기다리는 계획이 있으면 체크리스트가 있어도 정하기(읽기 전용)로 고쳤다. 계획 절이 없는 옛 기록은 그대로 모든 명령 허용이다.
- 전환 기록(`wiki-claude-obsidian-migration.md`)은 구현 계획을 현재 설계로 고쳐 재승인 단계에 있다. 재승인은 그 작업에서 받는다.

## 후속 · 2026-09-26

- 문구(사용자 "점검하다가 본 사소한 문구 올바르게 고쳐주세요."): 선택지가 없는 질문은 입력 칸 안내와 읽기 도구 이름을 답변 칸에 맞추고, 결과만 기록한 전달에는 처리 AI를 적지 않는다(`6bc7decc0`).
- 병렬 작업(사용자 "여러명의 프로그래머가 병렬로 task를 하나씩 맡아서 워크플로우를 각자 실행해도 merge할 때 문제 없을까요?"): 실제 서버 코드로 두 클론의 병합을 흉내 냈다. 서로 다른 작업은 깨끗이 병합되고, 같은 `.uasset`(LFS 잠금 없는 이진)·같은 작업 기록·같은 제목의 새 작업은 충돌한다. 충돌 표식이 남은 기록은 표가 표식 줄에서 끊겨 대시보드가 사람 항목 없이 읽는다. 사용자 결정: "이건 작업자끼리 서로 조심하도록 약속하면 되지 않을까요?" — 장치를 더하지 않고 약속 세 가지(한 작업 기록은 한 번에 한 사람, 구현 승인 전에 계획의 "어디에"를 보고 에셋 겹침 조율, 새 작업 제목은 구체적으로)로 한다.
- 도식(사용자 "작업 절차의 다이어그램이 실제 워크플로우와 일치하는지 점검해주세요."): 화살표 7개가 서버 동작과 일치했다. 만들기→정하기 화살표는 질문만 적혀 있었으나 추가 요청으로 계획이 바뀌어도 승인 줄이 지워져 정하기로 돌아가므로 「질문 · 계획 변경」으로 고쳤다. 완료된 작업의 패널에 남아 있던 터미널에서 이어하기는 Q8과 같은 이유(새 문제는 새 작업)로 숨겼다. 사용자 "고치는게 나을까요?"에 둘 다 고치기를 추천했고 "네"(`0ad6fd514`).

## 조사 · 2026-09-26 종합 점검

네 갈래 점검 보고와 AI의 재확인을 합쳤다. 표시: (확인) AI가 직접 재현·확인, (실행) 점검 중 실행으로 확인, (읽기) 코드 읽기로 추정.

### 결함

- D0 (확인, 처리함) `Export-Wiki.ps1`의 데이터 이스케이프가 `f9bf3de97`에서 효과 없는 치환으로 바뀌어, 기록에 `</script>`가 들어가면 대시보드가 멈추고 토큰이 든 페이지에 스크립트를 주입할 수 있었다. 순정 `-EscapeHandling EscapeHtml`로 고쳤다(`83eea4a94`).
- D1 (확인) Wiki 원자료 112건의 `refresh_due`가 모두 2026-10-26이다. `lint --as-of 2026-10-27`은 오류 343건("accepted claims require fresh active support")이고, 레저를 쓰는 모든 트랜잭션이 막힌다. README 2단계(수집 뒤, 10개씩)로는 풀리지 않고, 기한이 가까운 active 원자료 전부의 `refresh_due`만 트랜잭션 하나로 늦춰야 풀린다(점검 중 순정 검증기로 모의 실험).
- D2 (확인) 스킬 두 개가 작업 중인 AI에게 Wiki 직접 수정을 지시한다: `comment-cleanup/SKILL.md:73`, `module-review/SKILL.md:154`. 정본 145행("Wiki는 Wiki 갱신만 씁니다")과 어긋나고 플러그인 레저를 우회한다.
- D3 (확인) 접수 상태 파일 11개가 Git에 추적된다. 서버는 이 파일들을 예외 처리 없이 읽어 충돌 표식 하나로 시작하지 못하고, 저장된 프로세스 번호를 되살려 다른 프로세스와 겹치면 바쁨이 풀리지 않는다(`Workflow-TestFeedback.cjs:210, 218, 223`). 폐기된 `cleanup`·`blocked` 상태도 남아 있다.
- D4 (확인) `ability-directional-section.md`의 체크리스트 절 제목과 표 사이에 문단이 있어 해석기가 거부한다. 이 작업은 결과 전달과 추가 요청 반영이 막혀 있다. "표는 절 제목 바로 아래"라는 규칙은 코드에만 있다.
- D5 (실행) 화면의 보류 장치가 전송 실패 뒤 입력을 잠그고, 다시 보낼 버튼이 숨어 영구히 막히는 경우가 있다. 체크리스트 단계에서는 결과 대신 옛 추가 요청이 다시 전송된다.
- D6 (확인) 모듈 리뷰 기록 4건(WxAI·WxCombat·WxUI·WxGame)에 스킬이 금지한 상태 줄과 체크리스트가 있다. 스킬을 다시 돌리면 문서를 통째로 다시 써서 사용자 결정 원문과 대기 항목이 사라진다. WxGame은 사실상 구현 작업 기록이다.
- D7 (실행) 실패 수정 뒤 이전에 통과한 코드 리뷰가 그대로 유지되어, 고친 코드를 다시 리뷰받지 않고 완료될 수 있다. 정본 112행과 서버 동작이 다르다.
- D8 (실행·읽기) 3초 폴링마다 작업 진행 패널과 목록을 새로 그려 입력 칸의 포커스와 한글 조합이 끊긴다(서버 기록이 없는 작업에서 `revision` 비교가 매번 다름).
- D9 (읽기) 서버의 35분 제한은 AI를 끝내지 않고 잠금만 풀며, 상태 저장이 실패하면 잠금이 풀리지 않는다.
- D10 (읽기) 서버가 뜨지 못해도 옛 접속 정보가 페이지에 들어가 화면은 정상처럼 보이고, 오류는 영문 "Failed to fetch"로만 나온다.
- D11 (읽기) 질문 선택지를 한 번 고르면 해제할 수 없어, 직접 적은 답이 선택지와 합쳐져 결정으로 기록된다.
- D12 (실행·읽기) 브라우저 저장소를 못 쓰면 입력을 다 해도 전송이 막힌다. Wiki 갱신·터미널 버튼은 두 번 누르면 요청이 두 번 간다. 시각이 UTC 원문으로 보인다.
- D13 (실행) 래퍼가 저장소 드라이브 밖 경로와 역슬래시 상대 경로를 잘못 넘긴다. 엔진 확인 실패 메시지는 실제 원인(stderr)을 버린다.
- D14 (읽기) 터미널 인자 금지 문자(`&`·`^`·`<>`·`|`)가 과해, 사용자 폴더에 `&`가 있으면 모든 AI 처리가 실패한다. 큰따옴표 안이라 해석되지 않는 문자들이다.

### SSOT

- S1 코드 속 AI 요청문(`Workflow-TestFeedback.cjs:142-150, 163`, `Wiki-AI.cjs:81-84`)에 정본 규칙의 사본과 요청문에만 있는 규칙이 섞여 있다(Q1).
- S2 `process/index.md:145`가 Wiki 갱신 절차를 요약하고, `Wiki/README.md:8, 12, 53`이 누가 Wiki를 쓰는지·AI 고르기를 다시 적고, 루트 `README.md:5`가 Obsidian 여는 법을 다시 적는다. 정본의 출처 메모(151행)는 "Routine만 Wiki를 쓴다"로 낡았다.
- S3 스킬의 규칙 사본: `pull/skill.md:21, 23`(자동 백업 금지), `module-review/SKILL.md:15`(`Docs/Programmer` 읽기만), `:13`(없어진 목차 개념), `:154`(대시보드 자동 표시).
- S4 Wiki 노트가 워크플로우 규칙을 요약하고 폐기된 규칙을 확정 결정으로 싣는다(`Wiki/wiki/index.md:14`, `overview.md:13`, `concepts/작업 절차(Workflow).md:69, 89, 107`, `concepts/Wiki 운영.md:45`). 원본이 없는 옛 결정 노트에서 온 것이라 지금 절차로는 스스로 고쳐지지 않는다.
- S5 전환 기록의 승인된 구현 계획(토큰 버튼, `pull --rebase` 등)이 실제 구현(D9·D11·D12)과 다른데 승인 줄이 그대로다. 정본 141행은 계획이 바뀌면 다시 승인받게 한다.
- S6 Gemini CLI는 AGENTS.md를 읽지 않는다(기본 문맥 파일은 GEMINI.md). 대화로 시작한 Gemini는 정본 규칙을 모른다.

### 줄일 수 있는 장치

- M1 서버 교체 판단: 코드 파일 목록이 `Wiki-AI.cjs:13`과 `Start-WikiAI.ps1:8` 두 곳에 있고 `protocol`·`configuration` 필드는 아무도 읽지 않는다. "바쁘지 않으면 다시 띄운다" 하나로 줄일 수 있다.
- M2 서버 중복 장치: 35분 제한(실행기가 30분에 끝냄), `expectedRevision` 검사(`taskHash`와 중복), Gemini `mcp.allowed`(명령줄 인자와 중복), 도달하는 기록이 없는 파생 체크리스트 예외(정본 135행과 코드), 외부 사용처 없는 export, 요청문의 질문 번호 지시(서버가 다시 매김).
- M3 화면 잔재: 쓰이지 않는 navigation 계산·frontmatter 처리·"Project Knowledge" 문구·옛 CSS, 좁은 화면 메뉴 3칸 격자.
- M4 도식: 문서 하나의 도식 하나를 위한 자체 SVG→이미지 변환을 순정 `mermaid.run` 렌더로 바꿀 수 있다.
- M5 Wiki README를 순정 기준으로: 수집 판단은 캡처 사본 존재 대신 순정 기준(manifest와 원장), lint 관문은 순정 `--strict`, inbox 복사본 삭제 대신 커밋 범위를 `Wiki/wiki`·`Wiki/.raw`로 한정, 버전을 올린 뒤 첫 갱신에 순정 `migrate` 드라이런.
- M6 쓰지 않는 파일·설정: `.agents/skills/build-doctor/logs/.gitignore`, `pull/skill.md`의 소문자 이름, Routine에 붙은 커넥터 2개(`Claude_Docs`·`Claude_Code_Remote`).

### 그 밖

- E1 Wiki 절차의 빈 곳: 이름이 바뀌거나 지워진 기획서 처리, 날짜는 UTC, 원본은 바이트 그대로 복사, 무인 실행에서 페이지 삭제·병합·이름 변경 금지, 새 원자료가 옛 결정과 어긋날 때 순정 대체(`supersedes`)로 처리.
- E2 푸시 거절 규칙: 원격의 새 커밋이 `Wiki/`를 건드리지 않았으면 다시 수집할 필요 없이 rebase하면 된다. 낮에는 main 커밋이 잦아 대시보드 갱신이 자주 거절될 수 있다.
- E3 기록 정리: 확인 대기 기록의 AI 대기 항목(`checkpoint-savegame.md:32`, `spawner-library-removal.md:18`), 낡은 머리 서술(없어진 `.wiki` 경로, 이미 받은 리뷰를 "아직 없음"으로 적은 줄), 완료 기록의 중복 상태 줄, 중복된 추가 요청, 코드 리뷰 행이 없는 대기 기록(`damage-pipeline-structure-review.md`).
- E4 화면 보강: 작업 진행 패널에 기록의 다음 행동과 승인된 계획을 보이게 함, 사람 항목이 모두 통과하고 AI 항목만 실패일 때의 안내, 접근성(패널 포커스, 행 버튼 이름, 진행 알림).

### 유지가 맞다고 본 것

- sparse 작업 트리, `Saved/Workflow`의 claude-obsidian 태그 사본, `Wiki-Obsidian.cjs` 래퍼와 metadata 마운트, WSL 자동 설치: 순정으로 대신할 수단이 없거나 사용자 결정(전환 기록 D9·D10·D12)을 잃는다.
- 대시보드를 로컬 서버가 직접 제공하는 방식: 스냅샷 재생성과 토큰 만료 문제는 사라지지만, 지금 PowerShell이 하는 Markdown 변환을 Node로 옮기려면 새 의존성이 필요해 장치가 오히려 는다. 이번에는 하지 않는다.
- 작업 기록의 CRLF→LF 정규화, 경로 검사, 터미널 인자 인용, 실행기의 30분 종료, HTTP 토큰·Host 검사, 화면의 textContent 렌더와 링크 처리는 문제없었다.

## 통폐합 · 2026-09-27

- 사용자 요청("제가 확인해야되는 부분만 정리해서 새 일감을 만들고, 기존 일감은 적절히 통폐합합시다")으로 남은 확인을 [워크플로우 개선 마무리 확인](workflow-wrapup-checks.md)으로 모으고 이 기록을 닫았다. 사람 코드 리뷰는 이번에만 건너뛰고 AI 코드 리뷰로 대신했다(사용자 2026-09-27, 다음에는 사람이 리뷰한다).
- 코드 리뷰: AI 코드 리뷰로 바꿨다.
- Wiki 갱신 버튼: 두 번 누르기·진행 줄은 자동 테스트(TestWorkflowPage)가 확인하고, 실제 실행은 새 일감의 대시보드 Wiki 갱신으로 옮겼다.
