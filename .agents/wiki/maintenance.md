# Wiki 운영

상태: current · 범위: Wiki 운영 규약 · 2026-09-21

## 읽기

[Wiki](index.md)는 AI가 원자료를 확인하여 작성·축적하는 프로젝트 지식 DB다. 모듈·시스템·재사용할 결정과 작업 방법을 탐색한다. [Workflow](workflow/index.md)는 사람이 기획·설계·코드 리뷰·테스트 결과를 판단하는 공간이다. AI는 준비·구현·검증·기록·인계를 맡고, 완료 후 재사용할 지식을 Wiki에 통합한다. 사람의 판단 원문과 필요한 승인 근거는 보존한다.

상태와 확인 범위를 읽고, 실제 코드 변경에 필요한 주장은 링크된 원자료에서 다시 확인한다. `needs-review` 페이지는 탐색 지도이며 최신 구현의 보증이 아니다. 작업 가이드는 기획자와 AI를 위한 작업 방법 안내 전용이며 추후 추가한다. 새 모듈·시스템 문서는 아래 기술 참고 자료에, 새 리뷰는 in-progress 목록에 연결한다. Wiki 화면은 검색창·작업 단계 필터·검색 결과와 문서 열람에 집중한다. 사이드 메뉴·생성 시각·운영 안내는 표시하지 않으며 검색어와 필터가 없으면 전체 문서 목록을 펼치지 않는다. index.md는 AI의 지식 탐색 목차로 유지하고, Workflow 메뉴는 workflow/index.md에서 생성한다.

검색의 작업 단계는 기획·구현·테스트·완료이며 문서가 다루는 단계를 뜻한다. 작업 진행 상태나 승인 여부를 뜻하지 않는다. 문서에 `작업 단계: 기획, 구현`처럼 쉼표로 구분하여 명시하고, 여러 단계에 함께 분류할 수 있다. 단계 미지정 공통 문서는 모든 단계에서만 검색된다. Workflow 절차 문서도 Wiki 검색에 포함하며 열면 Workflow 화면으로 이동한다. 진행 중 인계·검토 자료는 Workflow 검색에서 찾는다. 문서 검증 상태는 검색 필터와 분리하여 문서 열람 시 표시한다.

## 로컬 웹 뷰어

[OpenWiki.bat](../../BatchFiles/OpenWiki.bat)은 AI 서버 없이 지식 검색 화면을 연다. [OpenWorkflow.bat](../../BatchFiles/OpenWorkflow.bat)은 로컬 AI 연결을 시작하고 작업·판단 화면을 연다. 두 실행기는 PowerShell 7을 사용하며, Workflow에는 Node.js와 선택한 AI CLI가 필요하다. 파일 첨부 변환의 Python 조건은 아래를 참고한다.

Markdown과 sources.json은 공용 정본이다. Export-Wiki.ps1은 Wiki를 Saved/Wiki/knowledge.html에, Workflow를 Saved/Wiki/index.html에 생성한다. Workflow는 기존 파일 URL과 브라우저 저장 키를 유지하여 미확정 입력·판단·저장 복구 기록의 이동을 피한다. 화면에서 서로 이동할 수 있으나, AI 연결이 없을 때는 OpenWorkflow.bat을 실행해야 한다. Wiki는 AI 인증 토큰·작업 상태 코드를 포함하지 않고 네트워크 연결도 차단한다. 지식 작성·갱신은 AI 작업에서 수행하며 Wiki 화면이 자율적으로 문서를 생성하는 기능은 아니다.

기획·설계의 AI 재검토 중에는 중앙 진행 안내와 회전 표시를 띄우고 배경 입력·메뉴 조작을 잠근다. 완료·실패 시 잠금을 풀고 결과 안내로 이동하며, 실패한 경우 기존 답변을 보존하고 재시도를 안내한다. 페이지 이탈·새로고침 시에는 브라우저의 이탈 확인을 요청한다. 판단 카드에는 확정 전(노랑), 답변 확정됨(초록)을 색상과 문구로 함께 표시한다. 확정 답변을 수정하면 확정 전 상태로 되돌아간다.

인계 저장 버튼 옆에는 저장 가능 여부와 필요한 조치를 표시한다. 미확정 답변·미해결 사항·재검토 필요 상태에서는 저장을 비활성화한다. 저장 중에는 중앙 진행 안내를 표시하고, 완료 후 인계 경로를 안내한다. 실패하면 구체적인 오류와 복구 방법을 유지한다. 사실·조사 근거와 자료·충돌의 별도 상세 섹션은 화면에 표시하지 않고 AI 검토·인계 자료로 보존한다.

문서 변경 후 실행기를 다시 실행한다. 문서 감시나 주기적 갱신은 하지 않는다. 검증 상태는 등록된 범위에 대한 표시이며 실시간 코드 검증 결과가 아니다. 생성 HTML은 저장소와 같은 범위에서 관리한다.

기획·구현 설계는 AI가 판단 자료 준비 → 사람이 항목별 답변 → AI가 결정 기록·통합 → AI 재검토 → 추가 판단만 질문하는 과정을 반복한다. 원문에 명확한 사실은 AI가 추출하고 사람에게 재입력시키지 않는다. 통합된 기획·설계는 사람이 최종 확정한다. 확정 답변은 보존하고 변경 영향이 있는 항목만 이유와 함께 다시 질문한다. AI가 원본·최종본·판단 기록·구현 및 검증 재료의 인계를 담당한다. 상세 절차는 [기획](workflow/planning.md)과 [구현](workflow/implementation.md)에 둔다.

입력·답변·초안·진행상황은 `.agents/in-progress/workflow_<작업 제목>_task.json`에 저장한다. 인증된 `/tasks` 조회와 `/task` 저장을 사용하며, 화면을 열면 파일에서 작업 목록을 읽어 기존 작업을 선택한다. 일반 리뷰 Markdown은 작업 목록에 포함하지 않는다. 브라우저에는 미전송 입력과 실패 요청의 복구 자료만 유지한다. 화면 진입·복귀와 연결 복귀 시 목록 조회·저장 결과 확인을 자동 수행한다. 사용자에게 새로고침·복구 버튼이나 별도 복구 작업 생성 기능을 제공하지 않는다. 작업 저장은 개정 번호로 오래된 덮어쓰기를 차단하고 동일 요청을 재전송해도 중복 적용하지 않는다. 과거 브라우저 목록은 읽지 않으며 자동 이관·중복 작업 생성은 하지 않는다. 최종 확정은 인증된 `/handoff`에서 작업·단계별 Markdown·JSON과 목록 링크를 만든다. 최종 확정본은 삭제·덮어쓰지 않는다. 변경은 `/change`로 새 작업을 생성해 이전 원본·판단·초안을 연결한다. 기존 작업의 공용 기록에 후속 작업을 연결하며 `node .agents/scripts/Wiki-AI.cjs --current "작업 제목"`는 이전·새 작업 어디에서 시작해도 변경 연결 전체를 따라 최신 기획·설계와 `pending` 보류 범위를 반환한다. 기획 변경이 확정되면 이전 설계를 새 기획에 적용하지 않으며, 설계만 변경하면 확정 기획을 그대로 이어받는다. 새 설계 확정 뒤 영향받는 구현·리뷰·테스트만 다시 수행한다. 기존 판단과 수용 근거는 과거 버전의 기록으로 보존한다.

기획·설계 분석은 기존 판단과 추가 의견을 함께 화면에서 선택한 Codex·Claude Code·Gemini CLI에 전달한다. 서비스별 읽기 전용 조사 제한과 응답 변환은 `.agents/scripts/Wiki-AI-Providers.cjs`에서 담당한다. 설계 분석은 관련 코드·설정·Wiki를 조사하고 확인한 근거와 자료 부족을 구분한다. 상태 모델과 서버가 같은 응답 검증을 사용하며 답변 변경 뒤 AI 재검토 전에는 최종 확정할 수 없다. 서버 코드를 갱신하면 실행기는 저장소 소유가 확인된 유휴 프로세스만 재시작한다. 기획·설계 검토에는 Node.js와 선택한 CLI의 설치·로그인이 필요하다. 구현·검증 실행에는 현재 Codex CLI가 필요하며 workspace-write 샌드박스를 사용한다. 설치된 CLI가 없으면 검토 버튼을 비활성화하지만 문서 열람과 로컬 인계 서버는 사용할 수 있다. 서비스별 인자·응답·실패 검증은 `node .agents/scripts/TestWikiProviders.cjs`, 모의 연결·인계 저장 검증은 `node .agents/scripts/TestWikiAI.cjs`, 실제 Codex 분석 검증은 같은 명령의 `--live` 또는 `--live-planning` 옵션과 실행 파일 경로를 사용한다. UI는 `node .agents/scripts/TestWikiViewer.cjs`로 반복 판단·보존·무효화·저장 실패·구버전 이관을 검사한다. 이 검사는 실제 모델 품질·브라우저 권한·게임 실행 검증이 아니다.

첨부 문서는 로컬 `/import`에서 Python으로 텍스트를 추출한다. Word(.docx) 본문·표, PDF 텍스트, PowerPoint(.pptx) 슬라이드 텍스트를 지원한다. 최대 20MB·추출 후 120,000자이며 이미지·스캔 OCR·도표·발표자 노트는 포함하지 않는다. UTF-8 Markdown·텍스트는 브라우저에서 읽는다. Python은 `WX_WIKI_PYTHON`, 현재 사용자 Codex 번들, PATH 순서로 찾으며 PDF에는 `pypdf`가 필요하다. 검증 명령은 `node .agents/scripts/TestWikiImport.cjs`다.

## 웹 작업 실행

작업 메뉴는 하나이며 현재 공용 기록에서 필요한 화면을 결정한다. 기획 확정은 설계 조사로, 설계 확정은 구현으로, 코드 리뷰 승인은 검증으로 이어진다. 최종 수용은 사람이 한다. 단계 선택·인계 문서 복사·명령 실행을 사용자에게 맡기지 않는다.

구현·검증 상태의 정본은 workflow_<제목>_execution.json이다. Workflow-Execution.cjs가 실행·결과·승인을 기록하고 /tasks가 초안과 확정 기록을 합쳐 읽는다. 브라우저 초안 저장은 실행 상태를 덮어쓰지 않는다. UI는 wiki-viewer/execution.js에서 결과와 판단만 표시한다. 상태를 여러 입력란에 중복 저장하지 않는다.

인증된 /execution은 동일 요청의 재전송으로 구현을 중복 실행하지 않는다. 실행 중에는 같은 저장소의 추가 구현과 확정 기준 변경을 막는다. 서버 중단은 중단 상태로 기록하고 이전 프로세스가 남아 있으면 저장소 전체의 추가 AI 실행을 막는다. 수정 요청은 판단 기록에 보존하며 실패 후 재시도에도 그대로 전달한다. 코드 리뷰·최종 수용은 결과를 생성한 코드 버전에 연결한다. 검증 중 코드가 변경되면 코드 리뷰로 돌아간다. 실패·미실행 검사를 통과로 바꾸지 않으며 미검증 수용에는 이유가 필요하다. 코드 검토용 diff는 현재 HEAD 대비 저장소 변경이며 기존 미커밋 변경도 포함한다.

TestWorkflowExecution.cjs는 실행 중복·중단·코드 변경·승인·미검증 수용·CLI 호출을 모의 검증한다. 실제 AI 응답 품질·UE 빌드·게임 플레이 성공을 보증하는 검사는 아니다.

## 원자료 수집과 갱신

한 작업은 한 사람이 편집하며 다른 사람은 공용 파일로 진행상황·결과를 확인한다. 별도 체크아웃·PC에는 작업 파일과 관련 확정 인계·공용 기록을 함께 전달해야 한다. 자동 Git 전송은 제공하지 않는다. 동시 편집 병합이나 충돌 선택 화면은 제공하지 않는다. 오래된 저장 요청은 거부하고 미전송 입력은 브라우저에 보존한다. 확정·변경 작업 생성·제목 변경 전에 요청 식별자·정확한 요청·반영할 입력 상태를 브라우저에 저장한다. 응답 유실이나 브라우저 저장 실패는 같은 요청으로 복구하며 이전 작업과 새 작업을 함께 보존한다. 변경 시작은 자식 기록을 준비한 뒤 상위 기록에 연결하므로 중간 실패 시 기존 기준은 유지된다. 연결되지 않은 자식은 구현 기준으로 쓰지 않는다. 확정 저장과 변경 연결은 개정 번호로 오래된 요청을 차단한다. 구버전의 미처리 철회 요청은 파일을 삭제하지 않고 복구한다. 이미 처리된 구버전 철회는 기존 처리 결과만 반환하며 삭제된 자료를 자동 복원하지 않는다. 관련 회귀 검사는 `TestWikiAI.cjs`와 `TestWikiRecovery.cjs`에 둔다.

Workflow 작업은 제목 자체(1~80자)를 식별자로 사용하며 별도의 내부 작업 ID가 없다. 공용 작업 파일, 공용 기록의 `taskId`, 이전·후속 작업 연결, `workflow_<작업 제목>_<단계>.md/.json` 파일명에 같은 제목을 쓴다. 한글·공백·괄호를 지원하고 파일명 금지 문자·끝의 점과 공백·중복 제목을 차단한다. 기존 작업은 제목으로 열고, 새 제목을 입력하면 새 작업을 만든다. 변경 작업 제목은 기존 제목과 요청 내용으로 자동 제안한다. **제목 변경**은 `/rename`에서 파일명·공용 참조·표시 제목을 함께 갱신하되 인간 판단·확정 내용·시각은 유지한다. 서버에 `workflow_rename_pending.json`을 먼저 저장하고 전체 갱신을 완료하기 전 다른 인계 사용을 막는다. 브라우저도 같은 요청으로 파일·부모·자식 작업 키를 복구한다. 구버전 내부 ID 작업은 제목 변경 경로로 이관하며 미처리 요청을 먼저 복구한다. 저장 요청의 재전송 식별자는 작업 식별자가 아니다. 인계가 일부 저장됐어도 공용 기록에 연결되지 않으면 구현 기준이 아니다.

1. 관련 Wiki와 코드·설정·기획 원자료를 읽고 변경이 설명에 미치는 영향을 판단한다. 파일 변경만으로 재검토나 문서 오류를 단정하지 않는다.
2. 필요한 설명만 갱신하고, 본문에 실제 근거 경로·확인 범위·기준 버전·미검증 사항과 중요한 판단을 남긴다.
3. `sources.json`은 뷰어용 페이지 메타데이터로 유지한다. `pages`의 Wiki 상대경로별 `status`, `scope`를 본문과 맞추고 기존 `baseline_commit`은 참고 기준으로 보존한다. 파일 해시·감시 루트·원자료 의존 목록은 관리하지 않는다.
4. 삭제·이동된 자료의 링크와 관련 목차를 정리한다.

작업 화면의 진행상황은 검토·답변·확정 기록에서 계산하며 별도 단계·메모를 입력받지 않는다. 첫 검토 전에는 기획서 입력만 표시하고, 추가 수정 의견은 검토 후 또는 저장된 의견이 있을 때 표시한다. 기존 진행 메모는 읽기 전용으로 보존한다. 작업 현황과 작업 편집 화면은 같은 상태 계산 함수를 사용한다.

## 링크 점검

저장소 루트에서 다음 명령으로 Wiki·작업 자료·안내 README의 로컬 Markdown 링크 대상 존재 여부만 확인한다.

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .agents/scripts/CheckWikiLinks.ps1
```

종료 코드는 정상 0, 링크 오류 1, 도구 실패 2다. 원자료 해시·Git 기준·문서 상태·목차 도달성은 검사하지 않는다. 섹션 앵커·외부 URL·문장 의미는 검사 범위 밖이다. 내용의 최신성은 AI가 관련 원자료와 대조해 판단한다.

## 작업 자료의 보존과 정리

AI는 작업 수행 자체를 장기 기록으로 남기지 않는다. 다음 작업에 필요한 현재 동작·구조적 제약·인간의 중요한 결정과 이유만 기존 Wiki 정본에 통합한다. 코드·설정·Git에서 바로 확인할 변경 나열, 조사 과정, 중간 초안, 해결된 오류, 일회성 빌드 성공은 별도 보고서로 만들지 않는다. 인간의 판단 원문을 AI 요약으로 대체하지 않는다.

작업 자료 파일은 후속 작업에 필요한 유효한 인계, 미해결 사항, 아직 필요한 리뷰·테스트 수용 근거가 있을 때만 유지한다. 생성 전에 같은 대상의 기존 문서를 찾고 그 파일을 갱신한다. 검토일·기준 버전·미검증 범위와 현재 필요한 이유를 본문에 표시하며, 해결된 지적 뒤에 수정 경위를 계속 덧붙이지 않고 현재 결론으로 다시 정리한다. 사용자가 보고서 산출을 명시한 경우에도 중복 생성과 종료 후 보존에는 같은 기준을 적용한다.

### 작업 재개

AI는 관련 작업 자료의 판단·미결정·코드 근거를 현재 원자료와 대조한다. 해결·대체된 내용은 현행 지적에서 제외하고, 영향받은 판단만 이유와 함께 다시 확인한다. 오래된 검토일만으로 결정을 무효화하거나 과거 검증을 현재 버전에 재사용하지 않는다. OpenWiki 인계는 공용 유효 기록 확인을 먼저 수행한다.

### 작업 완료

인간의 리뷰·테스트 수용 등 해당 작업의 완료 조건을 충족한 뒤 AI가 다음 정리를 수행한다.

1. 재사용할 동작·제약·결정 이유는 기존 Wiki 정본에 통합하고 원자료·판단 근거를 연결한다. 장기 가치가 없는 작업 내용은 옮기지 않는다.
2. 남은 미결정과 예외 수용·승인 근거는 기준 버전과 함께 필요한 최소 범위로 보존한다. 아직 필요한 자료의 위치와 보존 이유를 명시하고, 해결된 지적·중복·임시 자료를 제거한다. 남은 조치나 보존 이유가 없는 리뷰 문서는 유지하지 않는다.
3. 덮어쓰기·삭제로 사라질 인간 판단 원문이나 필요한 과거 근거는 실제 Git 커밋의 파일 내용 또는 별도의 유효한 정본에서 복원할 수 있는지 확인한다. Git 추적 여부만으로 보존됐다고 판단하지 않는다. 미커밋·미추적 자료는 복원 가능성을 확인할 때까지 유지하고 이유를 알린다. 정리는 자동 커밋·푸시 권한이 아니다.
4. 참조하는 Wiki·목차·페이지 메타데이터을 함께 갱신한다. OpenWiki가 현재 참조하거나 저장 복구 중인 인계 파일은 직접 삭제하지 않는다. 변경 연결과 복구를 확인하고, 확정본·판단 원문은 삭제하지 않는다. 현재 브라우저의 작업·복구 기록을 확인할 수 없으면 해당 자료를 유지한다.
5. 구조·링크 점검 후 정리 결과와 남긴 자료의 이유를 대화로 전달한다. 정리 보고서나 archive 폴더를 추가로 만들지 않는다.

### 정기 점검

정기 점검은 완료·재개 시 정리에서 빠진 중단·방치 작업을 찾는 보조 절차다. 프로젝트 정기 점검 때 중복 대상, 참조가 없는 문서, 완료·폐기 작업의 잔여 자료, 공용 유효 기록과 연결되지 않은 인계, 해결된 지적을 확인한다. 기간 경과나 링크 부재만으로 자동 삭제하지 않는다. 작업 상태를 알 수 없으면 AI가 근거를 조사하고 필요한 완료·폐기 판단만 사람에게 요청한다. 후보와 이유는 대화로 전달하고 실제 정리는 위의 보존·참조 확인 절차를 따른다. 별도 예약 실행이나 자동 삭제 기능을 뜻하지 않는다.

## 기존 도구와의 관계

`Docs/Programmer/`는 프로그래머가 직접 관리하는 문서만 둔다. AI는 읽기·인용만 하고 자동 생성·수정·삭제하지 않는다. `.agents/in-progress/`에는 실제 진행 중인 작업 자료만 둔다. 경로 이관은 코드 재검증이나 완료·폐기 판정이 아니며 기존 기준 커밋과 검토일을 유지한다.

in-progress의 시점별 보고서는 모듈·시스템 정본과 구분한다. 같은 대상을 다시 검토하면 `module_review_<Module>.md`처럼 대상마다 하나인 파일을 갱신하고 날짜를 파일명에 넣어 새 파일을 만들지 않는다. 검토일과 기준 커밋은 본문 머리에 남긴다. 링크 검사는 수행하지만 과거 보고서 자체를 최신 구현으로 표시하지 않는다. 현재 페이지가 보고서를 근거로 인용하면 본문에서 해당 자료와 확인 범위를 연결한다. 보고서에 포함된 주장은 가능한 한 코드·기획 원자료로 재확인한다.

작업마다 worklog를 새로 작성하지 않는다. 파일·코드 변경 내역은 Git 커밋/PR, 현재 구조는 모듈·시스템 페이지, 재사용할 설계 결정은 decisions, 미결정은 관련 검토 결과 문서의 확인할 사항 섹션에 기록한다. 모든 작업을 Wiki에 기록할 필요는 없다. Wiki 갱신 이력은 Git에서 확인하며 별도 갱신 로그를 작성하지 않는다.

기존 worklog는 2026-09-20 사용자 요청으로 모두 삭제했다. worklog 폴더를 다시 만들거나 새 기록을 작성하지 않는다.

과거 판단이 필요하면 Git 이력에 남아 있는 자료를 찾아 현재 코드와 대조한다. Git에 보존되지 않은 기록의 복구를 보장하지 않는다. 삭제 전에 전체 기록을 Wiki로 이관하지는 않았다. 과거의 완료·빌드 성공을 현재 버전의 검증 결과로 옮기지 않는다.

`readme-writer`는 호환 이름을 유지하되 Wiki 모듈 페이지를 갱신한다. `module-review`는 후속 조치에 필요한 결과만 `.agents/in-progress/module_review_<Module>.md`에 남긴다. 발견과 보존 이유가 없으면 대화로 전달한다. 리뷰의 제안은 Wiki에 결정으로 자동 반영하지 않는다.

공용 WxCore·Config·기획 변경은 관련 설명에 미치는 영향을 AI가 확인한다. 에셋 설정을 설명할 때에는 실제 원본과 추출 근거·엔진/추출기 버전·미확인 범위를 본문에 남긴다.

여러 작업자가 갱신하면 본문 파일 단위로 작업을 나누고, `sources.json`·목차는 한 작업자가 합친다. 상시 문서 감시와 원격 검색 서버는 도입하지 않았으며 로컬 AI 연결만 사용한다.

## 기술 참고 자료

모듈·시스템의 구현 설명은 아래에서 탐색한다.

### 모듈

아래 9개는 기존 README에서 이관한 모듈 설명입니다. 최신 상태·기준 커밋·검증 범위는 각 페이지와 sources.json에서 확인합니다.
- [WxAI](modules/WxAI.md)
- [WxCombat](modules/WxCombat.md)
- [WxCore](modules/WxCore.md)
- [WxDialogue](modules/WxDialogue.md)
- [WxInventory](modules/WxInventory.md)
- [WxQuest](modules/WxQuest.md)
- [WxUI](modules/WxUI.md)
- [WxWorld](modules/WxWorld.md)
- [WxGame](modules/WxGame.md)

WxEditor, WxToolset, BoxComponentVisualizerEditor는 기존 README 이관 대상이 없었으며 아직 모듈 페이지가 없습니다. 프로젝트 전체 문서화가 완료되었다는 뜻은 아닙니다.

### 시스템과 미결정

- [그로기](systems/groggy.md) — 기획과 정적 C++ 진입·종료 경로, 에셋·실행 미검증
- [그로기 피니시](systems/finisher.md) — 대상 선정·이동 주체 기획 충돌로 blocked
- [전투 자원](systems/combat-resources.md) — 공통 기획과 현광 구현 보고의 구분
- [피니시 미결정](systems/finisher.md#확인할-사항) — Q-001~002
- [전투 자원 미결정](systems/combat-resources.md#미결정) — Q-003
- [GameplayTag 선언 위치 결정](decisions/gameplay-tag-ownership.md) — Q-004 종료(2026-09-21)

### 기존 검증 기록

아래는 질문 목록에서 보존한 기존 검증 기록이며, 이번 이관에서 수행한 재검증은 아니다.

기존 모듈 설명 9개는 이관 후 2026-09-20 핵심 계약·주요 C++ 경로를 재검증해 해당 범위의 `current`로 갱신했다. WxDialogue의 해시 변경 원자료 4개도 대조했다. 전체 소스 결함 검토·빌드·게임 실행·바이너리 에셋 내부는 포함하지 않으며, 페이지별 검증 절을 확인한다.
