# 스캔 타이머 콜백에 Handle 접두사 적용

## 계획

### 목표
`module_review_WxWorld.md` 5 항목. `FTimerManager::SetTimer` 에 바인딩되는 `ScanAndPush` 가 코딩 규칙 4(델리게이트 콜백은 `Handle` 접두사)를 어긴다. `HandleScanTimer` 로 개명한다.

### 수정 범위
| 파일 | 수정할 내용 | 구분 |
|---|---|---|
| `Plugins/WxWorld/Source/WxWorld/Public/Interaction/WxInteractionScannerComponent.h` | `ScanAndPush` 선언 → `HandleScanTimer` | 수정 |
| `Plugins/WxWorld/Source/WxWorld/Private/Interaction/WxInteractionScannerComponent.cpp` | 정의·`SetTimer` 바인딩·`BeginPlay` 즉시 1회 호출의 이름 갱신 | 수정 |

### 접근 방식
- **함수 분리 없이 개명만**: 리뷰가 제시한 "폴링 진입점과 스캔 본문 분리"는 취하지 않는다. 호출처가 타이머와 `BeginPlay` 즉시 1회 둘뿐이고 둘 다 같은 일을 원하므로, 함수를 쪼개면 한 줄짜리 위임만 늘어난다.

---

## 완료

### 수정한 파일
| 파일 | 수정한 내용 | 구분 |
|---|---|---|
| `Plugins/WxWorld/Source/WxWorld/Public/Interaction/WxInteractionScannerComponent.h` | `ScanAndPush` → `HandleScanTimer` (선언 1곳) | 수정 |
| `Plugins/WxWorld/Source/WxWorld/Private/Interaction/WxInteractionScannerComponent.cpp` | 정의·`SetTimer` 바인딩·`BeginPlay` 즉시 호출 3곳 갱신 | 수정 |

### 구현·결정과 그 이유
- **개명만 했다**: 호출처가 타이머 바인딩과 `BeginPlay` 즉시 1회 둘뿐이고 둘 다 같은 동작을 원한다. 진입점/본문을 쪼개면 위임 한 줄만 늘어난다.
- **선언 주석은 그대로 뒀다**: "소유 클라에서 주기 타이머로 호출된다"는 개명 후에도 정확하다.

### 계획 대비 달라진 점
- 계획대로.

### 후속 과제
- 없음. WxEditor(Development) 빌드 `Result: Succeeded`.
