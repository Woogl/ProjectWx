# 장거리 좌표 이동 치트

## 계획

- `UWxCheatManager`에 `WxTeleport(float X, float Y, float Z)` Exec 치트를 추가한다.
- 플레이어 폰의 현재 회전을 유지하고 충돌 보정 없이 지정한 월드 좌표로 텔레포트해 월드 스트리밍 장거리 이동을 시험할 수 있게 한다.
- `BatchFiles/BuildProjectFiles.bat`으로 UE 5.8 `WxEditor` Development 빌드를 검증한다.
- 현재 작업 트리의 관련 없는 변경은 건드리지 않는다.

## 완료

- `UWxCheatManager`에 콘솔에서 `WxTeleport X Y Z` 형식으로 호출하는 좌표 이동 치트를 추가했다.
- 플레이어 폰의 회전을 유지하면서 `TeleportTo`의 충돌 보정을 끄고 입력한 월드 좌표를 정확히 적용하도록 구현했다.
- 성공 시 실제 이동 좌표를, 실패 시 목표 좌표를 `LogWxGame`에 남기도록 했다.
- UE 5.8 `WxEditor` Win64 Development 빌드에 성공했다.
- 빌드 로그: `C:\Wx\Saved\Logs\BuildDoctor\build_2026-09-03_010911_393_4136.log`
