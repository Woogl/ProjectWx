# WxDevice StateTree 저작 구조 개선

## 계획

- 사용자 후속 지시: 기획 미확정 단계의 저작 검증은 복잡도를 높이므로 도입하지 않는다. 이번에 추가한 전용 Schema·검증 코드·상태 유지 태스크는 제거하고 템플릿 제작은 보류한다.
- 상태에 귀속되는 상호작용과 초기화·복구 시 일회성 효과 억제에 변경을 집중한다. 초기 상태·늦은 접속에서는 이동 목표 위치를 적용한다.
- 기존 Wx.Device 회귀 테스트와 필요한 동작 검증, UE 5.8 WxEditor Win64 Development 빌드로 확인한다.

## 완료

- 새 전용 Schema·에셋 검증·상태 유지 태스크를 제거했다. 에디터 등록 변경도 되돌렸다. 새 자동화 테스트나 템플릿은 추가하지 않았다.
- 복구 판정을 공통 함수로 모아 기존 일회성 태스크에 적용했다. 장치 이동 태스크는 복구 중 목표 위치를 즉시 적용한다.
- 상호작용 태스크는 상태 종료 시 자기 바인딩을 제거하며 활성 부모 설정이 있으면 복원한다. 초기 상태 목록에 이름과 태그를 함께 표시한다.
- UE 5.8 WxEditor Win64 Development 빌드 성공: `Saved/Logs/BuildDoctor/build_2026-09-06_014645_091_15500.log`.
- 기존 Wx.Device 자동화 9개 성공, 실패·경고 0: `Saved/Automation/WxDevice/index.json` (2026-09-06 01:48 KST). `git diff --check` 통과.
- 실제 에셋의 연출과 다중 프로세스 접속은 미검증이다. 기존 에셋에서 상태를 떠난 뒤에도 상호작용 설정이 유지된다고 가정한 구성은 필요한 상태/공통 부모에 태스크를 배치해야 한다.
