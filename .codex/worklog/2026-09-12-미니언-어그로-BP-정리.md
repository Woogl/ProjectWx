# 미니언 어그로 BP 정리

## 계획

- 사용자 승인: 이번 어그로 관련 사용자 BP 구현을 제거하고 제안한 IsAggroIgnored 방식으로 교체한다.
- BP 미구현 기본값 false가 어그로 허용이 되도록 IWxMinion과 AI 소비 경로를 IsAggroIgnored로 전환한다.
- BP_Minion의 CanBeAggroTarget 그래프를 제거하고 IsAggroIgnored=true, GetMaxCountPerMaster=1로 구성한다. 다른 변경은 유지한다.
- UE 5.8 WxEditor Development 빌드와 저장 BP의 반환값 검증을 수행한다.

## 완료

- IWxMinion의 BP 이벤트를 IsAggroIgnored로 교체했다. 기본 false는 어그로 허용이며 true는 AI 타겟 선택·유지에서 제외한다.
- BP_Minion의 IsAggroIgnored=true, GetMaxCountPerMaster=1 그래프를 구성해 컴파일·저장했다. 이전 CanBeAggroTarget 그래프 부재를 확인했다.
- UE 5.8 WxEditor Win64 Development 빌드 성공(종료 코드 0). 로그: Saved/Logs/BuildDoctor/build_2026-09-12_060139_756_35700.log.
- 별도 프로세스로 저장 BP를 다시 로드·컴파일하여 인터페이스 구현, IsAggroIgnored=true, 상한 1, 옛 그래프 부재를 assert 검증했다. 최종 commandlet 종료 코드 0. 로그: Saved/Logs/MinionAggroVerify.log.
- 수정한 코드·설정의 git diff --check 통과. 다른 작업의 변경은 유지했으며 PIE 전투 재현은 수행하지 않았다.
