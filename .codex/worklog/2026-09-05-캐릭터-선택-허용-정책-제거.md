# 캐릭터 선택 허용 정책 제거

## 계획

- 승인에 따라 bAllowCharacterSelection과 관련 분기를 제거한다.
- FrontEnd가 전달한 Pawn 클래스가 있으면 사용하고, 없으면 Experience.DefaultPawnClass 또는 기존 Spectator를 사용한다.
- 맵·Pawn·Experience의 유효성과 기존 전환 처리는 유지한다.
- UE 5.8 WxEditor Development 빌드와 선택 진입·직접 PIE 기본값 경로를 검증한다.

## 완료

- bAllowCharacterSelection 선언과 GameMode·GameFlow 조건을 제거했다. 선택 Pawn → Experience 기본 Pawn → 기존 Spectator 순서로 처리한다. Experience와 맵·선택 클래스 유효성 검사는 유지했다.
- EXP_Combat 에셋을 다시 저장해 제거한 속성을 정리하고 관련 주석·현재 설계 문서를 갱신했다.
- UE 5.8 WxEditor Win64 Development 빌드 성공(종료 코드 0, Result: Succeeded). 로그: C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-05_205416_730_19016.log.
- NullRHI PIE에서 테스트 프로세스의 Experience 기본 Pawn만 Spectator로 바꾼 뒤, 두 맵 모두 선택한 BP_Player로 진입함을 확인했다. 선택 유지·중복 차단·FrontEnd 왕복·입력 복원도 통과했다. 로그: C:/Wx/Saved/Logs/PawnSelectionPriority.log. 기본 Pawn 변경은 저장하지 않았다.
- 선택 없이 LV_DevCombat을 직접 PIE 실행하면 Experience 초기화 후 BP_Player가 생성됨을 확인했다. 로그: C:/Wx/Saved/Logs/DirectPawnDefault.log. 초기 임시 Spectator를 판정하던 테스트는 Experience 초기화 완료를 기다리도록 정정했다.
- 소스·설정·현재 설계 문서 및 EXP_Combat 에셋에서 제거 변수 참조 없음 확인. diff 공백 검사 통과. 실제 렌더링과 패키지는 검증하지 않았다.
