# 소스 BOM 통일 배치 파일

## 계획

- 게임 소스의 UTF-8 NO BOM 정책에 따라 BatchFiles/NormalizeBOM.bat을 추가한다.
- 프로젝트와 플러그인의 Source 아래 .h/.cpp/.cs를 처리하며 생성물과 외부 소스는 제외한다.
- UTF-8 유효성을 확인한 뒤 선두 BOM만 제거하고 본문 바이트와 줄바꿈을 보존한다. 다른 인코딩은 오류로 보고한다.
- 변경 없는 --check 옵션과 처리 결과를 제공한다.
- 임시 파일로 검사 모드, BOM 제거, 본문 보존, 오류 처리, 반복 실행을 검증하고 UE 5.8 WxEditor Win64 Development 빌드를 확인한다.

## 완료

- BatchFiles/NormalizeBOM.bat 단일 파일로 구현했다. 기본 실행은 UTF-8 BOM을 제거하며 --check는 검사만 수행한다.
- 종료 코드: 0=정상, 1=검사에서 BOM 발견, 2=인자/인코딩/처리 오류. 생성물, ThirdParty, 링크는 제외한다.
- 임시 프로젝트에서 한글과 혼합 줄바꿈의 바이트 보존, 중복 BOM 및 BOM만 있는 파일 처리, 제외 경로, 검사 모드 무변경, 반복 실행 무변경, 잘못된 UTF-8 및 NUL 거부, 잘못된 인자 처리를 검증했다.
- 실제 소스 검사: 499개 중 BOM 2개, 오류 0개. 실제 소스에는 검사만 실행했다.
- 최초 샌드박스 빌드는 UBT 시작 직후 종료되었다. build-doctor를 샌드박스 외 실행하여 UE 5.8 WxEditor Win64 Development 빌드 성공(종료 코드 0)을 확인했다.
- 빌드 로그: C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-12_170955_142_32872.log
- git diff --check 통과.
