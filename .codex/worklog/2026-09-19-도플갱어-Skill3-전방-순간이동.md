# 도플갱어 Skill3 전방 순간이동

## 계획

- 사용자 최종 승인: MirrorAbility의 이번 순간이동 변경을 제거하고 MirrorMovement의 분신 AbilityActivatedCallbacks에서 처리한다. 태그와 500cm 거리는 서비스의 BT 설정으로 둔다. 순간이동 실패 시에도 스킬은 계속 발동한다.
- BT_Doppelganger에서 Ability.Skill.3 모방 시 마스터 전방 500cm로 순간이동하고 마스터를 바라본 뒤 발동한다.
- 해당 스킬 실행 중 추적 이동·회전 보정을 중단하고 종료 후 기존 추적 동작으로 복귀한다.
- 재시도의 중복 순간이동을 방지하고 다른 스킬 동작은 유지한다.
- UE 5.8 WxEditor Development 빌드 및 가능한 동작 검증을 수행한다.

## 완료

- MirrorMovement가 분신 ASC의 AbilityActivatedCallbacks를 구독해 지정 스킬 발동 시 순간이동한다. 알림은 ActivateAbility/몽타주 시작 전 전달된다.
- FaceMasterAbilityTags와 AbilityTeleportDistance를 추가하고 BT_Doppelganger에 Ability.Skill.3, 500cm로 저장했다. 스킬 중 추적 입력을 생략하고 마스터를 바라보며 종료 후 기존 추적으로 복귀한다.
- TeleportTo 배치 검사 실패 시에도 스킬은 발동한다.
- MirrorAbility의 이번 순간이동 설정·헬퍼·호출 변경은 제거했다. 작업 전 존재하던 선입력 재시도 변경은 보존했다.
- UE 5.8 WxEditor Development 빌드 성공. 로그: `C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-19_061942_213_6872.log`.
- `Wx.AI.MirrorMovement.AbilityTeleport` 자동화 테스트 통과. 기본 설정에서 위치 미변경, yaw 기준 500cm 이동, 방향·속도, 스킬 중 추적 입력 중단, 종료 후 위치·방향 복귀 검증. 로그: `C:/Wx/Saved/Logs/MirrorMovementAbilityTeleportTest.log`.
- 최초 테스트는 테스트용 BT 노드 메모리 누락으로 중단됐다. 테스트 메모리 수정 후 통과했다.
- 사용자 에디터 종료 후 BT 에셋 저장 성공(0 errors). 로그: `C:/Wx/Saved/Logs/ConfigureDoppelgangerSkill3.log`.
- `git diff --check -- Plugins/WxAI` 통과. 플레이 몽타주 연출의 시각 검증은 미실시.
- 새 Development 프로세스에서 저장된 BT 설정을 다시 읽어 `WX_SKILL3_READBACK_SUCCESS` 확인. 검증 후 Development 에디터 재실행.
