# FrontEnd 로딩 표시 제거

## 계획

- 사용자 정정에 따라 요청 범위 밖의 SetTravelLoadingMessage, 로딩 오버레이와 연결 호출·진행 문구를 제거한다.
- 선택 UI, 중복 입장 방지, 도착 준비 및 실패 복구는 유지한다. 로딩 UI는 후속 작업으로 남긴다.
- 설계 문서를 정정하고 UE 5.8 WxEditor Development 빌드 및 기존 전환·복구 검증을 실행한다.

## 완료

- SetTravelLoadingMessage, TravelLoadingWidget, SetLoadingVisible 및 호출과 진행 문구를 제거했다. UIManagerSubsystem은 기존 코드로 복원되었다.
- 설계 문서에서 로딩 UI를 후속 작업으로 명시했다. 선택·전환·도착 준비·복구 처리는 유지했다.
- UE 5.8 WxEditor Win64 Development 빌드 성공(종료 코드 0). 로그: C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-05_201551_646_7484.log (`Result: Succeeded`). 추가 빌드 수정 불필요.
- NullRHI PIE: 두 목적지 진입, FrontEnd 복귀, 중복 입장 방지, 입력 복원 성공. 결과: C:/Wx/Saved/FrontEndPlayResults.json.
- NullRHI PIE: 진입점 누락 시 FrontEnd 복귀 및 재시도 활성화 성공. 결과: C:/Wx/Saved/FrontEndRecoveryResult.txt. 화면 렌더링 검증은 수행하지 않았다.
