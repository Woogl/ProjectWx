# Ability 쿨다운 월드 타이머 전환

## 계획

- 승인된 변경안에 따라 CoreTicker를 ASC 월드의 TimerManager로 교체한다.
- SetTimerForNextTick 재예약으로 매 프레임 갱신을 유지하고, 쿨다운 종료·재초기화·해제 시 예약을 정리한다.
- 기존 쿨다운 계산과 충전 복제 판정은 유지한다.
- 변경 diff와 타이머 수명 경로를 확인하고 런처에서 조회한 UE 5.8로 WxEditor Win64 Development를 빌드한다.

## 완료

- CoreTicker 의존성을 제거하고 StartCooldownTimer/StopCooldownTimer 및 단발 타이머 콜백으로 교체했다.
- 쿨다운이 남아 있으면 SetTimerForNextTick으로 재예약하며, 종료·어빌리티 교체·Deinitialize에서 정리한다. 예약 월드의 약한 참조를 보관해 ASC 소멸 뒤에도 해제할 수 있다.
- 기존 잔여 시간·충전 스택 계산을 유지하고 미사용 DeltaTime 인자를 제거했다.
- git diff --check 통과, 관련 파일의 Ticker 참조 제거와 타이머 등록·종료 경로 확인.
- 초기 제한 환경 빌드가 진단 없이 종료되어 build-doctor를 승인된 샌드박스 외 실행으로 재실행했다. UE 5.8.2 WxEditor Win64 Development 빌드 성공(종료 코드 0).
- 빌드 로그: C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-15_012910_388_11976.log
- PIE 실행 검증은 수행하지 않았다.
