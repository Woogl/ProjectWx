# 인벤토리 정적 조회 헬퍼 제거

## 계획

- 승인한 설계대로 FindInventory 정적 헬퍼를 제거하고 호출처에서 PlayerController의 FindComponentByClass를 사용한다.
- 뷰모델은 관찰 시작 시 직접 조회하고 BeginPlay 여부를 확인한다. 이후 Ready가 전달한 소스에 연결하고 Ended에서는 연결만 해제한다.
- 플레이어당 인벤토리 하나를 종료 후 교체하는 수명을 기준으로 테스트를 정리하고 UE 5.8 WxEditor Development 빌드, 인벤토리 회귀 테스트 및 위젯 컴파일을 확인한다.

## 완료

- FindInventory 선언·정의와 전체 호출처 참조를 제거했다. 각 호출처에서 PlayerController의 FindComponentByClass를 사용한다.
- 뷰모델은 최초 조회 결과를 BindSource의 HasBegunPlay/파괴 여부 검사로 걸러낸다. Ready는 전달받은 인스턴스에 직접 연결하며 Ended는 현재 연결만 해제한다. 이후 Ready 구독은 유지한다.
- 등록됐지만 BeginPlay 전인 소스와 종료됐지만 소유자에 남아 있는 소스를 최초 조회에서 거부하는 검증을 추가했다. 새 소스 Ready는 기존 종료 소스가 남아 있어도 전달된 새 인스턴스에 연결함을 확인했다.
- UE 5.8 WxEditor Win64 Development 빌드 성공(종료 코드 0). 로그: `C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-05_233415_604_23868.log`. 근거: `Result: Succeeded`, `BUILD_DOCTOR_RESULT=success`.
- Wx.MVVM.Inventory 테스트 2개 성공, 실패/경고 0. 보고서: `C:/Wx/Saved/Automation/InventoryDirectLookup/index.json`.
- 기존 인벤토리 관련 위젯 6개 컴파일 및 BS_UP_TO_DATE 확인. 로그: `C:/Wx/Saved/Logs/InventoryDirectLookupWidgets.log`. 커맨드릿 경고 1개는 기존 MCP 플러그인 안내이며 컴파일 오류는 없다.
- git diff --check 통과. 실제 네트워크 지연 PIE는 실행하지 않았다. 직접 조회는 PlayerController당 인벤토리 하나를 기준으로 하며, 둘을 동시에 유지하는 교체 수명은 지원 범위가 아니다.
- 빌드 재실행: `& 'C:/Wx/.agents/skills/build-doctor/scripts/Invoke-WxEditorBuild.ps1' -ProjectRoot 'C:/Wx'`. 실행기가 출력한 명령: `& 'C:/Program Files/Epic Games/UE_5.8/Engine/Build/BatchFiles/Build.bat' WxEditor Win64 Development '-Project=C:/Wx/Wx.uproject' -WaitMutex -NoHotReloadFromIDE`. 추가 조치는 필요 없다.
