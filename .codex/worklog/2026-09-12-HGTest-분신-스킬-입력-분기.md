# HGTest 분신 보유 상태에 따른 E 키 스킬 분기

## 계획

- 분신 생성·사망·소멸에 따라 주인의 `State.Minion.Active` 태그를 갱신한다.
- `GA_HGTest_Skill_1`은 분신 미보유 시, `GA_HGTest_Skill_2`는 보유 시에만 같은 E 키 입력으로 발동하도록 설정한다.
- 기존 작업 변경 사항을 보존한다. 분신 소멸 후 스킬 1 복귀와 중복 발동 여부를 검증하고 UE 5.8 WxEditor Development 빌드를 확인한다.

## 완료

- `UWxMinionSubsystem`이 소환 성공, 소환물 사망·EndPlay, 주인 EndPlay 때 주인의 `State.Minion.Active`를 갱신한다. 태그는 TagOnly로 복제한다.
- `Skill_1/GA_HGTest_Skill_1`의 ActivationBlockedTags와 `Skill_2/GA_HGTest_Skill_2`의 ActivationRequiredTags에 해당 태그를 설정했다. 두 에셋의 기존 `IA_Skill_1` 입력은 유지했다.
- 두 스킬의 `bRetriggerInstancedAbility`를 끄고, 실행 중 콤보 재발동이 태그 조건을 건너뛰는 경로를 막았다. 두 블루프린트 컴파일·저장을 완료했다.
- UE 5.8.2 WxEditor / Win64 / Development 빌드 성공(종료 코드 0). 로그: `C:/Wx/Saved/Logs/BuildDoctor/build_2026-09-12_062718_887_11872.log`.
- `Wx.Combat.HGTest.MinionSkillRouting` 자동화 테스트 성공, 오류 0. 실제 분신 BP 생성, 보유 상태별 스킬 태그 조건의 배타성, 소멸·사망 후 스킬 1 복귀, 시체 제거 시 상태 유지, 주인 파괴 시 정리를 검증했다. 테스트 월드의 AI 블랙보드 관련 경고 21건은 남아 있다.
- 테스트 보고서: `C:/Wx/Saved/Automation/HGTestSkillRouting/index.json`. 검증은 별도 무화면 프로세스에서 수행했으며, 실제 E 키 조작·멀티플레이 복제 시나리오는 별도로 실행하지 않았다.
- 기존 사용자 변경 사항은 보존했다.
